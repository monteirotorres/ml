# Naive Bayes

O Naive Bayes aplica o teorema de Bayes assumindo, de forma "ingênua", que os preditores são independentes dada a classe. Essa suposição raramente é verdadeira, mas o modelo funciona surpreendentemente bem e é extremamente rápido.

É o clássico para classificação de texto (spam, triagem de laudos) e um ótimo veículo para revisar probabilidade condicional. Veremos as variantes Gaussiana, Multinomial e Bernoulli e quando cada uma se aplica.

> **Em construção.** Este tópico terá conteúdo completo, notebook interativo e
> slides em breve. Abaixo, o que o material cobrirá.

| O notebook cobrirá | Detalhe |
| --- | --- |
| Teorema de Bayes na prática | priori, verossimilhança e posteriori |
| Variantes do modelo | Gaussiano, Multinomial, Bernoulli |
